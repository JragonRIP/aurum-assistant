# Aurum Voice Engine — local Kokoro-82M
# Create: python -m venv .venv
# Install: python -m pip install -r requirements.txt
# espeak-ng is bundled via espeakng-loader (no system MSI required).

kokoro>=0.9.4
espeakng-loader>=0.2.4
soundfile>=0.12.1
misaki[en]>=0.9.0
numpy>=1.24.0
